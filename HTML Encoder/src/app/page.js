import Footer from "@/components/HTMLEncoder/footer";
import Navbar from "@/components/HTMLEncoder/navbar";
import FAQSection from "@/components/HTMLEncoder/faqsection";
import HtmlEncoder from "@/components/HTMLEncoder/htmlEncoder";


export default function Home() {
  return (
    <div style={styles.container}>
      <header>
        <Navbar />
      </header>
      <main style={styles.mainContent}>
        
<HtmlEncoder/>
<br></br>
        <FAQSection/>
      </main>
      <footer style={styles.footer}>
        <Footer />
      </footer>
    </div>
  );
}
const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    minHeight: '100vh',
  },
  mainContent: {
    flex: '1',
    padding: '20px',
  },
  footer: {
    marginTop: 'auto',
  },
};

